{{- if .Values.enhancedTrafficManagement.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ include "sandbox.controller.name" . }}
  name: {{ include "sandbox.controller.name" . }}
  namespace: {{ include "sandbox.namespace" . }}
spec:
  {{- if not .Values.enhancedTrafficManagement.gatewayController.autoscaling.enabled }}
  replicas: {{ .Values.enhancedTrafficManagement.gatewayController.replicas | default 1 }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ include "sandbox.controller.name" . }}
  strategy:
    rollingUpdate:
      maxSurge: 100%
      maxUnavailable: 25%
  template:
    metadata:
      annotations:
        prometheus.io/port: "15014"
        prometheus.io/scrape: "true"
      labels:
        app: {{ include "sandbox.controller.name" . }}
    spec:
      containers:
      - args:
        - discovery
        - --monitoringAddr=:15014
        - --log_output_level=default:info
        - --domain
        - {{ .Values.enhancedTrafficManagement.global.trustDomain }}
        - --keepaliveMaxServerConnectionAge
        - 30m
        env:
        - name: REVISION
          value: default
        - name: PILOT_CERT_PROVIDER
          value: istiod
        - name: POD_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.namespace
        - name: SERVICE_ACCOUNT
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: spec.serviceAccountName
        - name: KUBECONFIG
          value: /var/run/secrets/remote/config
        - name: PILOT_ENABLE_AMBIENT
          value: "true"
        {{- if .Values.enhancedTrafficManagement.ambient.enabled }}
        - name: CA_TRUSTED_NODE_ACCOUNTS
          value: {{ include "sandbox.namespace" . }}/{{ include "sandbox.ztunnel.name" . }}
        {{- end }}
        - name: PILOT_TRACE_SAMPLING
          value: "1"
        - name: PILOT_ENABLE_ANALYSIS
          value: "false"
        - name: CLUSTER_ID
          value: "{{ .Values.enhancedTrafficManagement.global.clusterId }}"
        - name: GOMAXPROCS
          valueFrom:
            resourceFieldRef:
              divisor: "1"
              resource: limits.cpu
        
        - name: MESH_CONFIG_MAP_NAME
          value: {{ .Values.enhancedTrafficManagement.gatewayController.resourcePrefix | quote }}
        - name: INJECTOR_CONFIG_MAP_NAME
          value: {{ printf "%s-sidecar-injector" .Values.enhancedTrafficManagement.gatewayController.resourcePrefix | quote }}
        - name: LEADER_ELECTION_PREFIX
          value: {{ .Values.enhancedTrafficManagement.gatewayController.resourcePrefix | quote }}
        - name: ENABLE_SANDBOX_CONTROLLER
          value: "true"
        {{- if .Values.enhancedTrafficManagement.gatewayController.onDemandCertSignMode }}
        - name: ON_DEMAND_SIGN_MODE
          value: {{ .Values.enhancedTrafficManagement.gatewayController.onDemandCertSignMode | quote }}
        {{- end }}
        {{- if .Values.enhancedTrafficManagement.gatewayController.restrictedSecretsScope }}
        - name: RESTRICTED_SECRETS_SCOPE
          value: {{ include "sandbox.namespace" . }}
        {{- end }}
        - name: PILOT_HTTP10
          value: "true"
        - name: ENABLE_ON_DEMAND_CERTS
          value: "{{ .Values.enhancedTrafficManagement.gatewayController.enableTlsTermination }}"
        - name: PILOT_ENABLE_IP_AUTOALLOCATE
          value: "false"
        - name: MESH_INTERNAL_TRAFFIC_POLICY
          value: {{ .Values.enhancedTrafficManagement.global.meshInternalTrafficPolicy | default "PEER_AWARE" | quote }}
        - name: AMBIENT_ENABLE_MULTI_NETWORK_WAYPOINT
          value: "false"
        - name: PILOT_ENABLE_GATEWAY_API
          value: "false"
        - name: PILOT_ENABLE_GATEWAY_API_STATUS
          value: "false"
        - name: INJECT_ENABLED
          value: "{{ .Values.enhancedTrafficManagement.sidecarInjector.enabled }}"
        - name: PILOT_ENABLE_CA_CRL
          value: "false"
        - name: PILOT_CA_CERT_CONFIGMAP
          value: sandbox-ca-certs
        - name: AMBIENT_ENABLE_STATUS
          value: "false"
        - name: PILOT_IGNORE_RESOURCES
          value: "*."
        - name: PILOT_INCLUDE_RESOURCES
          value: "trafficpolicies.network.alibabacloud.com,globaltrafficpolicies.network.alibabacloud.com"
        - name: ISTIOD_CUSTOM_HOST
          value: {{ include "sandbox.controller.name" . }}.{{ include "sandbox.namespace" . }}.svc,{{ include "sandbox.controller.name" . }}.{{ include "sandbox.namespace" . }}.svc.cluster.local
        {{- if .Values.enhancedTrafficManagement.sidecarInjector.enabled }}
        - name: INJECTION_WEBHOOK_CONFIG_NAME
          value: {{ .Values.enhancedTrafficManagement.gatewayController.resourcePrefix }}-sidecar-injector-{{ include "sandbox.namespace" . }}
        {{- end }}
        {{- if .Values.enhancedTrafficManagement.gatewayController.krtEventDistributeDebounce }}
        - name: KRT_EVENT_DISTRIBUTE_DEBOUNCE
          value: {{ .Values.enhancedTrafficManagement.gatewayController.krtEventDistributeDebounce | quote }}
        {{- end }}
        {{- if .Values.enhancedTrafficManagement.gatewayController.krtEventDistributeDebounceMax }}
        - name: KRT_EVENT_DISTRIBUTE_DEBOUNCE_MAX
          value: {{ .Values.enhancedTrafficManagement.gatewayController.krtEventDistributeDebounceMax | quote }}
        {{- end }}
        {{- range $key, $val := .Values.enhancedTrafficManagement.gatewayController.env }}
        - name: {{ $key }}
          value: {{ $val | quote }}
        {{- end }}
        image: {{ include "sandbox.image" (dict "hub" .Values.enhancedTrafficManagement.gatewayController.image.hub "name" .Values.enhancedTrafficManagement.gatewayController.image.name "tag" .Values.enhancedTrafficManagement.gatewayController.image.tag "globalHub" .Values.enhancedTrafficManagement.global.hub) }}
        name: discovery
        ports:
        - containerPort: 8080
          name: http-debug
          protocol: TCP
        - containerPort: 15010
          name: grpc-xds
          protocol: TCP
        - containerPort: 15012
          name: tls-xds
          protocol: TCP
        - containerPort: 15017
          name: https-webhooks
          protocol: TCP
        - containerPort: 15014
          name: http-monitoring
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 1
          periodSeconds: 3
          timeoutSeconds: 5
        resources:
          {{- if .Values.enhancedTrafficManagement.gatewayController.resources }}
          {{- toYaml .Values.enhancedTrafficManagement.gatewayController.resources | nindent 10 }}
          {{- else }}
          requests:
            cpu: 500m
            memory: 2Gi
          limits:
            cpu: 500m
            memory: 2Gi
          {{- end }}
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
        volumeMounts:
        - mountPath: /var/run/secrets/tokens
          name: istio-token
          readOnly: true
        - mountPath: /var/run/secrets/istio-dns
          name: local-certs
      serviceAccountName: {{ include "sandbox.controller.name" . }}
      {{- with .Values.enhancedTrafficManagement.gatewayController.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.enhancedTrafficManagement.gatewayController.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.enhancedTrafficManagement.gatewayController.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
      - emptyDir:
          medium: Memory
        name: local-certs
      - name: istio-token
        projected:
          sources:
          - serviceAccountToken:
              audience: istio-ca
              expirationSeconds: 43200
              path: istio-token
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ include "sandbox.controller.name" . }}
  name: {{ include "sandbox.controller.name" . }}
  namespace: {{ include "sandbox.namespace" . }}
spec:
  ports:
  - name: grpc-xds
    port: 15010
    protocol: TCP
  - name: https-dns
    port: 15012
    protocol: TCP
  - name: https-webhook
    port: 443
    protocol: TCP
    targetPort: 15017
  - name: http-monitoring
    port: 15014
    protocol: TCP
  selector:
    app: {{ include "sandbox.controller.name" . }}
{{- if .Values.enhancedTrafficManagement.gatewayController.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "sandbox.controller.name" . }}
  namespace: {{ include "sandbox.namespace" . }}
  labels:
    app: {{ include "sandbox.controller.name" . }}
spec:
  maxReplicas: {{ .Values.enhancedTrafficManagement.gatewayController.autoscaling.maxReplicas | default 5 }}
  minReplicas: {{ .Values.enhancedTrafficManagement.gatewayController.autoscaling.minReplicas | default 2 }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "sandbox.controller.name" . }}
  metrics:
  {{- if .Values.enhancedTrafficManagement.gatewayController.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.enhancedTrafficManagement.gatewayController.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.enhancedTrafficManagement.gatewayController.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.enhancedTrafficManagement.gatewayController.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
  {{- if .Values.enhancedTrafficManagement.gatewayController.autoscaling.behavior }}
  behavior:
    {{- toYaml .Values.enhancedTrafficManagement.gatewayController.autoscaling.behavior | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
